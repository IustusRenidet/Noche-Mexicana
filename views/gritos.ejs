<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ranking de Gritos Mexicanos</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>¡El Mejor Grito Mexicano!</h1>
        
        <!-- Formulario para agregar un nuevo grito -->
        <form action="/gritos/agregar" method="POST">
            <input type="text" name="grito" placeholder="Escribe tu grito mexicano" required>
            <button type="submit">Agregar Grito</button>
        </form>

        <!-- Ranking de gritos -->
        <h2>Ranking</h2>
        <ul>
            <% gritos.forEach(grito => { %>
                <li>
                    <%= grito.texto %> - Votos: <%= grito.votos %>
                    <form action="/gritos/votar/<%= grito.id %>" method="POST" style="display:inline;">
                        <button type="submit">Votar</button>
                    </form>
                </li>
            <% }) %>
        </ul>
        <a href="/">Volver al Menú</a>
    </div>
</body>
</html>